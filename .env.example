# Server-side only — never prefix with NEXT_PUBLIC_, or Next.js will
# bundle it into client JS and it becomes visible in the browser.
ANTHROPIC_API_KEY=sk-ant-...

# Optional override for lib/ai/config.ts's default model.
CHAT_MODEL_ID=claude-sonnet-4-6
